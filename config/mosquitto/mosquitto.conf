# Minimal, internal-only broker. No ports are published; only other containers can reach it.
listener 1883
allow_anonymous true

# Keep retained messages in RAM to spare the SD card.
persistence true
persistence_location /mosquitto/data/

# Send logs to stdout (Docker will rotate per compose settings).
log_dest stdout
